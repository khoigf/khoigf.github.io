# Từ @BotFather: /newbot
TELEGRAM_BOT_TOKEN=

# Chat ID nhóm (gõ /id trong nhóm sau khi thêm bot)
TELEGRAM_CHAT_ID=

# Bot quỹ riêng (tuỳ chọn). Để trống = gắn lệnh quỹ vào bot vé cơm ở trên.
QUY_BOT_TOKEN=
QUY_CHAT_ID=

# Gom thay đổi quỹ rồi mới commit GitHub (mặc định: 8 lệnh hoặc 3 phút)
# QUY_FLUSH_OPS=8
# QUY_FLUSH_MS=180000

# Múi giờ lịch gửi /ve và nhắc quỹ
TZ=Asia/Ho_Chi_Minh

# URL public của server (Render tự có RENDER_EXTERNAL_URL; local thì để trống)
PUBLIC_URL=

# PAT GitHub Contents: Read and write — để sơ đồ/quỹ không mất khi server restart
GITHUB_TOKEN=
GITHUB_REPO=khoigf/khoigf.github.io

# Chỉ khi muốn bot chụp trang khác, không phải chính server này
# VECOM_URL=https://khoigf.github.io/vecom/
